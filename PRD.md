# Danmu Agent 多平台用户体验优化方案

---

## **一、背景与目标**
Danmu Agent 弹幕陪伴插件覆盖 **全平台视频网站**（如 YouTube、B站、腾讯视频、Netflix、Vimeo 等），需实现以下目标：
1. **跨平台兼容性**：支持主流视频网站的弹幕抓取与处理。
2. **统一交互体验**：用户界面和功能在不同平台保持一致性。
3. **性能优化**：确保多平台运行时的内存占用（≤150MB）和延迟（≤800ms）。
4. **合规性**：符合各平台数据抓取政策及隐私法规。

---

## **二、技术实现方案**

### **1. 弹幕抓取模块扩展**
- **通用 DOM 解析器**  
  - **平台适配层**：为每个目标平台编写独立的 **DOM 解析脚本**。
  - **动态加载**：通过 `chrome.webRequest` 检测当前页面域名，加载对应解析脚本。
  - **示例代码**：
    ```javascript

    ```

- **备用方案**  
  - 对于无公开 API 的平台（如 Netflix），采用 **OCR 技术** 或 **用户授权的 API**。

---

### **2. 视频播放同步机制**
- **时间轴对齐**  
  - 使用 `video.currentTime` 监听播放进度，结合弹幕时间戳实现同步。
  - **拖拽进度条补偿**：动态调整弹幕播报时间轴。
  - 示例代码：
    ```javascript
    const video = document.querySelector("video");
    video.addEventListener("timeupdate", () => {
      const currentTime = video.currentTime;
      // 触发弹幕筛选与播报逻辑
    });
    ```

---

### **3. AI 动态筛选与语音播报**
- **模型部署**  
  - Qwen2.5-Omni 模型通过 **ONNX.js** 在浏览器端运行，无需云端服务。
  - **平台专属模式**：  
    - **B站**：启用“直播模式”，语音风格活泼，配合弹幕“高能预警”。  
    - **Netflix**：启用“电影模式”，过滤低俗弹幕。

- **多语言 TTS**  
  - 集成 Azure TTS 或 Web Speech API，支持中英德法等语言。

---

### **4. 用户界面 (Popup) 扩展**
- **平台标识与自动检测**  
  - 显示当前平台标识（如 YouTube/B站/腾讯视频），支持手动切换。
  - 自动检测逻辑：
    ```javascript
    const detectPlatform = () => {
      const hostname = window.location.hostname;
      if (hostname.includes("youtube.com")) return "youtube";
      if (hostname.includes("bilibili.com")) return "bilibili";
      // 其他平台判断逻辑...
    };
    ```

- **平台专属功能**  
  - **B站**：支持弹幕“高能预警”。  
  - **Netflix**：提供“亲子模式”（过滤低俗弹幕）。

---

## **三、用户体验优化策略**

### **1. 设计一致性**
- **视觉与交互统一**  
  - 界面布局、颜色、字体、图标保持一致，降低学习成本。
- **功能逻辑统一**  
  - 核心功能（弹幕抓取、筛选、语音播报）操作流程一致。
- **信息呈现一致性**  
  - 弹幕样式（字体大小、颜色、滚动速度）统一，支持多语言自动切换。

---

### **2. 交互逻辑优化**
- **操作路径标准化**  
  - 所有平台的“关闭弹幕”按钮位于右上角。
- **动态适配输入方式**  
  - 移动端支持手势滑动关闭弹幕，桌面端支持快捷键。

---

### **3. 性能优化**
- **Web Worker**：将弹幕筛选、模型推理任务移至 Web Worker。
- **缓存机制**：对高频弹幕文本进行本地缓存。
- **模型量化**：使用 ONNX.js 的 WebAssembly SIMD 优化，单条弹幕处理 ≤50ms。

---

### **4. 合规性与隐私保护**
- **数据最小化**：仅抓取当前播放视频的弹幕数据，不存储浏览历史。
- **本地化处理**：Qwen2.5-Omni 模型在浏览器端运行，避免云端传输敏感数据。
- **平台政策适配**：  
  - 对禁止自动化抓取的平台（如 Netflix），提供 OCR 备用方案或 API 接入。

---

## **四、测试与验证**

### **1. 跨平台兼容性测试**
| **平台** | **测试指标** | **目标值** |
|----------|--------------|------------|
| YouTube | 弹幕抓取成功率 | ≥95% |
| B站     | 时间轴同步误差 | ≤1.5 秒 |
| Netflix | 内存占用       | ≤150MB |

---

### **2. 用户场景测试**
1. **B站二次元番剧**：验证情感分析模型对“萌点”弹幕的识别能力。  
2. **Netflix 恐怖片**：测试“高能预警”功能的触发准确性。  
3. **YouTube 教育视频**：验证多语言翻译的准确性（如中文→英文）。

---

## **五、未来扩展方向**
1. **硬件联动**：与智能音箱/LED 灯带联动，实现跨设备弹幕反馈。  
2. **社交功能**：允许用户分享精选弹幕至社交媒体（如 Twitter/X）。  
3. **AR/VR 集成**：在虚拟现实环境中投射弹幕，增强沉浸式体验。

---

## **六、附录**
- **平台适配器示例代码**：[GitHub 仓库链接](https://github.com/your-username/Danmu-Agent)  
- **合规性文档**：[GDPR 合规声明](https://docs.google.com/document/d/1p2-urcDrVfXHxHW7OyIawo4EwJB_wy6wA2WvTrtAMDU/edit)
